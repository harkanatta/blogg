[build]
  publish = "public"
  # $URL is set by Netlify, so deploy previews and branch deploys
  # get the right absolute URLs too (canonifyurls is on).
  command = "hugo --gc --minify -b $URL"

[build.environment]
  HUGO_VERSION = "0.166.0"
